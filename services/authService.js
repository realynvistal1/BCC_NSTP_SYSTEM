/**
 * Authentication helpers are currently implemented in authController.js. Move reusable auth/database functions here as the module grows.
 *
 * The current controllers remain functional; this service file is the dedicated
 * location for reusable logic when that controller logic is extracted.
 */
module.exports = {};
