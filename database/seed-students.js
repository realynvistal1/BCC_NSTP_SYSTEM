/**
 * Optional student seed file.
 *
 * Student accounts are normally created through the Enrollment Form, so this
 * file intentionally does not create fake students by default. Add controlled
 * test records here only when you need development data.
 */
console.log("Student seeding skipped: create students through /enrollment for realistic testing.");
